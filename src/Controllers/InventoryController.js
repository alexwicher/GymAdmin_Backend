class InventoryController{
    static getInventoryById(request, reply) {
        return Promise.resolve(undefined);
    }

    static getInventoryOfFacility(request, reply) {
        return Promise.resolve(undefined);
    }

    static createInventoryInFacility(request, reply) {
        return Promise.resolve(undefined);
    }
}