class ActivityController {
    static getActivityById(request, reply) {
        return Promise.resolve(undefined);
    }

    static getAllActivities(request, reply) {
        return Promise.resolve(undefined);
    }

    static createActivity(request, reply) {
        return Promise.resolve(undefined);
    }

}