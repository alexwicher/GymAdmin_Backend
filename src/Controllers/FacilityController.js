class FacilityController{
    static getAllFacilities(request, reply) {
        return Promise.resolve(undefined);
    }

    static getFacilityById(request, reply) {
        return Promise.resolve(undefined);
    }

    static createFacilitySchedule(request, reply) {
        return Promise.resolve(undefined);
    }

    static getFacilitySchedule(request, reply) {
        return Promise.resolve(undefined);
    }

    static createFacility(request, reply) {
        return Promise.resolve(undefined);
    }

    static getFacilityActivities(request, reply) {
        return Promise.resolve(undefined);
    }
}