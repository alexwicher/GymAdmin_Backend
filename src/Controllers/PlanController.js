class PlanController{

    static getAllPlans(request, reply) {
        return Promise.resolve(undefined);
    }

    static getPlanById(request, reply) {
        return Promise.resolve(undefined);
    }

    static createPlan(request, reply) {
        return Promise.resolve(undefined);
    }

    static createPlanSchedule(request, reply) {
        return Promise.resolve(undefined);
    }

    static getPlanSchedule(request, reply) {
        return Promise.resolve(undefined);
    }
}